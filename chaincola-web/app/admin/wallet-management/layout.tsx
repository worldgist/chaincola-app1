import WalletManagementShell from './WalletManagementShell';

export default function WalletManagementLayout({ children }: { children: React.ReactNode }) {
  return <WalletManagementShell>{children}</WalletManagementShell>;
}
